# Copy to .env and adjust
API_PORT=8000
# Absolute path to this repo (defaults to app/api/../../..). Override in Electron/Docker/Colab.
# APPLIO_ROOT=C:/Users/Pascal/Desktop/Applio
# Python that has the Applio env (torch, librosa, ...). `run-install.sh/bat` sets this up.
PYTHON_BIN=python
# Where uploads + converted audio live (defaults to assets/audios)
# UPLOADS_DIR=
# OUTPUTS_DIR=
